1. Implement a function to find the longest substring without repeating characters.

2. Factorial of a number

3. Given array of strings, return array where anagram strings are adjacent.

4. Two sums

5. Develop an API utilizing a Class equipped with two distinct methods. The initial method is designed to record and store a user's PING (timestamp). Subsequently, the second method facilitates retrieving the PING of a user within a specified time range and partition.

6. The task required assigning specific numbers of agents to tasks. Once all agents had assignments, the one with the oldest task had to be eliminated and replaced with a new task. Additionally, I needed to generate a list of the next available agents for further assignments, covering the next N tasks.

7. Given two arrays of numbers, find the intersection, union, and difference, returning results as arrays of unique values.

8. Create a function that groups an array of objects by multiple properties and returns a nested object structure representing the grouping hierarchy.

9. Given an array of products with categories and prices, implement a function that calculates the average price per category and returns an object with category names as keys and average prices as values.